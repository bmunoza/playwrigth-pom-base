{
  "name": "playwright-pom-base",
  "version": "1.0.0",
  "private": true,
  "description": "Base Playwright project in TypeScript using Page Object Model (POM).",
  "scripts": {
    "test": "playwright test",
    "compile": "tsc --noEmit",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/node": "^20.11.30",
    "typescript": "^5.9.3"
  }
}
